{% extends 'base.html.twig' %}

{% block title %}Projet{% endblock %}

{% block body %}
    <h1>Projet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ projet.id }}</td>
            </tr>
            <tr>
                <th>IntituleMarche</th>
                <td>{{ projet.IntituleMarche }}</td>
            </tr>
            <tr>
                <th>Domaine</th>
                <td>{{ projet.Domaine }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ projet.Montant }}</td>
            </tr>
            <tr>
                <th>Contrat</th>
                <td>{{ projet.contrat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('projet_index') }}">back to list</a>

    <a href="{{ path('projet_edit', {'id': projet.id}) }}">edit</a>

    {{ include('projet/_delete_form.html.twig') }}
{% endblock %}
